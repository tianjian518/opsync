# taosync-lite 部署到 Hugging Face Docker Space
#
# 这就是「只改两个文件」的关键：本 Space 的 Dockerfile 只需一行，
# 基于已经推送到 Docker Hub 的预构建镜像。把 <你的DockerHub用户名>
# 换成你自己的 Docker Hub 用户名即可，无需往 Space 里放任何源码。
FROM docker.io/<你的DockerHub用户名>/taosync-lite:latest
