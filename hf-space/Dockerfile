# opsync 部署到 Hugging Face Docker Space
# Web 管理界面监听 $PORT（默认 7860），配置 / 触发 / 日志都在浏览器完成。
# 只改这一行即可：基于已推送到 Docker Hub 的预构建镜像（pin 到 digest 确保拉到新构建）。
FROM docker.io/tianjian518/opsync:latest@sha256:4e40b7d1a683e41652d52340329c0716d761297721fe51490e68a69b058b508b
